v-env
.env
!.env.example

node_modules
__pycache__
v-env
.DS_Store
aws_cf_deploy_anything_llm.json
yarn.lock
*.bak
.idea

# Sample data: binary fixtures kept local-only (see samples/*/README.md).
samples/**/raw/

# Local Claude Code workspace config (per-machine, not shared).
.claude/

# Generated at runtime by ContextWindowFinder; not source.
server/__tests__/utils/agents/models/context-windows/

# Local scratch page for manually exercising the embed widget.
/test.html
